services:
  manasik-app:
    build: .
    container_name: manasik_container
    network_mode: "host"
    environment:
      - DISPLAY=${DISPLAY:-host.docker.internal:0}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix